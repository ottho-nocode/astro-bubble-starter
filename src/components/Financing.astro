---
const financingOptions = [
  {
    title: "CPF (Compte Personnel de Formation)",
    description: "Vos droits CPF expirent si vous ne les utilisez pas. Nos formations sont eligibles : financez votre reconversion a 100%, sans avancer un euro.",
    icon: "wallet",
  },
  {
    title: "France Travail (ex Pole Emploi)",
    description: "Demandeur d'emploi ? France Travail peut prendre en charge la totalite de votre formation. On s'occupe du dossier avec vous.",
    icon: "building",
  },
  {
    title: "OPCO",
    description: "Salarie ? Votre entreprise peut financer votre montee en competences via son OPCO. Aucun reste a charge pour vous.",
    icon: "briefcase",
  },
];
---

<section class="py-24 lg:py-32 relative">
  <div class="relative z-10 mx-auto max-w-6xl px-6">
    <div class="flex flex-col lg:flex-row gap-12 lg:gap-20 items-center">
      <!-- Left: Content -->
      <div class="flex-1">
        <div class="reveal">
          <div class="inline-flex items-center gap-2 rounded-full bg-green-50 border border-green-200/60 px-4 py-1.5 mb-6">
            <div class="w-1.5 h-1.5 rounded-full bg-green-500 animate-pulse"></div>
            <span class="text-xs font-semibold text-green-700 uppercase tracking-wider">Financable a 100%</span>
          </div>

          <h2 class="text-3xl sm:text-4xl lg:text-5xl font-heading font-extrabold text-gray-900 leading-tight mb-4">
            Ne laissez pas le prix vous <span class="bg-gradient-to-r from-green-600 to-emerald-500 bg-clip-text text-transparent">freiner</span>
          </h2>
          <p class="text-lg text-gray-500 font-body mb-10">
            97% de nos apprenants ne paient rien de leur poche. Verifiez votre eligibilite en 2 minutes.
          </p>
        </div>

        <div class="space-y-4">
          {financingOptions.map((option, i) => (
            <div class={`reveal reveal-delay-${i + 1} group flex gap-4 rounded-2xl border border-gray-200/60 bg-white p-5 shadow-sm hover:shadow-md hover:border-gray-300 transition-all duration-200`}>
              <div class="flex-shrink-0 w-11 h-11 rounded-xl bg-gradient-to-br from-green-50 to-emerald-50 border border-green-200/40 flex items-center justify-center">
                {option.icon === "wallet" && (
                  <svg class="w-5 h-5 text-green-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                    <path d="M21 12V7H5a2 2 0 010-4h14v4" stroke-linecap="round" stroke-linejoin="round" />
                    <path d="M3 5v14a2 2 0 002 2h16v-5" stroke-linecap="round" stroke-linejoin="round" />
                    <circle cx="18" cy="14" r="2" />
                  </svg>
                )}
                {option.icon === "building" && (
                  <svg class="w-5 h-5 text-green-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                    <path d="M3 21h18M5 21V7l8-4v18M19 21V11l-6-4" stroke-linecap="round" stroke-linejoin="round" />
                    <path d="M9 9v.01M9 12v.01M9 15v.01M9 18v.01" stroke-linecap="round" stroke-linejoin="round" />
                  </svg>
                )}
                {option.icon === "briefcase" && (
                  <svg class="w-5 h-5 text-green-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                    <rect x="2" y="7" width="20" height="14" rx="2" stroke-linecap="round" stroke-linejoin="round" />
                    <path d="M16 7V5a2 2 0 00-2-2h-4a2 2 0 00-2 2v2" stroke-linecap="round" stroke-linejoin="round" />
                  </svg>
                )}
              </div>
              <div>
                <h3 class="font-heading font-bold text-gray-900 mb-1 text-sm">{option.title}</h3>
                <p class="text-sm text-gray-500 leading-relaxed">{option.description}</p>
              </div>
            </div>
          ))}
        </div>

        <div class="reveal mt-8">
          <a
            href="/prendre-rdv"
            class="btn-press group relative inline-flex items-center gap-2 rounded-full px-7 py-3.5 text-base font-semibold text-white overflow-hidden transition-transform duration-150 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-green-400 focus-visible:ring-offset-2 focus-visible:ring-offset-[#FAFAF8]"
          >
            <div class="absolute inset-0 bg-gradient-to-r from-green-600 to-emerald-500"></div>
            <div class="absolute inset-0 bg-gradient-to-r from-emerald-500 to-green-400 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
            <span class="relative z-10">Verifier mon eligibilite en 2 min</span>
            <svg class="relative z-10 w-4 h-4 group-hover:translate-x-0.5 transition-transform duration-200" viewBox="0 0 16 16" fill="none">
              <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
          </a>
          <p class="text-xs text-gray-400 mt-2">Gratuit &middot; Sans engagement &middot; Reponse immediate</p>
        </div>
      </div>

      <!-- Right: Visual Card -->
      <div class="flex-1 max-w-md reveal reveal-delay-2">
        <div class="relative">
          <!-- Glow -->
          <div class="absolute inset-0 bg-gradient-to-br from-green-200/30 to-emerald-200/20 blur-3xl rounded-full scale-125"></div>

          <div class="relative rounded-3xl border border-gray-200/60 bg-white shadow-xl shadow-gray-900/5 p-10 overflow-hidden">
            <div class="relative z-10">
              <p class="text-xs font-semibold text-gray-400 uppercase tracking-[0.2em] mb-3">Financement</p>
              <p class="text-6xl font-heading font-extrabold text-gray-900 mb-1">100%</p>
              <p class="text-xl font-heading font-bold text-gray-600 mb-6">pris en charge</p>
              <div class="w-full h-px bg-gray-200 mb-6"></div>
              <p class="text-sm text-gray-500 leading-relaxed">
                Grace au CPF, France Travail ou votre OPCO, votre formation peut etre entierement financee.
              </p>
            </div>
          </div>

          <!-- Floating badge -->
          <div class="absolute -bottom-4 -right-4 rounded-xl border border-gray-200/60 bg-white shadow-lg px-4 py-3 flex items-center gap-2.5">
            <div class="w-8 h-8 rounded-lg bg-green-50 flex items-center justify-center">
              <svg class="w-4 h-4 text-green-600" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
              </svg>
            </div>
            <span class="text-sm font-semibold text-gray-900">Eligible CPF</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
