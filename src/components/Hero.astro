---
const slides = [
  { name: "Simon", caption: "De boulanger a developpeur No Code freelance", img: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=600&h=450&fit=crop&q=80" },
  { name: "Pierre", caption: "De paysagiste a Product Builder chez The Tribe", img: "https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=600&h=450&fit=crop&q=80" },
  { name: "Fanny", caption: "De specialiste gestion de projet a fondatrice de Planote", img: "https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=600&h=450&fit=crop&q=80" },
];
---

<section class="relative min-h-screen flex items-center overflow-hidden pt-20">
  <!-- Background gradient mesh (light) -->
  <div class="absolute inset-0">
    <div class="absolute top-[-20%] left-[-10%] w-[60%] h-[60%] rounded-full bg-primary-200/30 blur-[120px]"></div>
    <div class="absolute bottom-[-10%] right-[-10%] w-[50%] h-[50%] rounded-full bg-purple-200/20 blur-[100px]"></div>
    <div class="absolute top-[30%] right-[20%] w-[30%] h-[30%] rounded-full bg-primary-100/25 blur-[80px]"></div>
    <!-- Grid pattern -->
    <div class="absolute inset-0 opacity-[0.04]" style="background-image: linear-gradient(rgba(0,0,0,.06) 1px, transparent 1px), linear-gradient(90deg, rgba(0,0,0,.06) 1px, transparent 1px); background-size: 60px 60px;"></div>
  </div>

  <div class="relative z-10 mx-auto max-w-6xl px-6 py-16 lg:py-24 w-full">
    <div class="flex flex-col lg:flex-row items-center gap-12 lg:gap-16">
      <!-- Left Content -->
      <div class="flex-1 max-w-[560px] text-center lg:text-left">
        <!-- Badge -->
        <div class="reveal inline-flex items-center gap-2 rounded-full bg-white shadow-sm border border-gray-200/60 px-5 py-2.5 mb-8">
          <div class="w-1.5 h-1.5 rounded-full bg-green-500 animate-pulse"></div>
          <span class="text-sm font-medium text-gray-700">L'ecole en ligne n-1 sur Bubble en</span>
          <span class="text-lg" aria-label="France">&#x1F1EB;&#x1F1F7;</span>
        </div>

        <!-- H1 -->
        <h1 class="reveal reveal-delay-1 text-4xl sm:text-5xl lg:text-6xl font-heading font-extrabold tracking-tight leading-[1.1]">
          <span class="text-gray-900">Devenez developpeur </span>
          <span class="bg-gradient-to-r from-primary-600 via-purple-600 to-primary-500 bg-clip-text text-transparent">Bubble</span>
          <span class="text-gray-900"> sans coder une seule ligne</span>
        </h1>

        <!-- Subtitle -->
        <p class="reveal reveal-delay-2 mt-6 text-lg lg:text-xl text-gray-500 leading-relaxed font-body max-w-lg">
          Nos bootcamps intensifs vous forment en 10 semaines. 93% de taux de completion, financables CPF, et une communaute de 3&nbsp;000+ alumnis.
        </p>

        <!-- CTAs -->
        <div class="reveal reveal-delay-3 mt-10 flex flex-col sm:flex-row items-center gap-4">
          <a
            href="/#formations"
            class="btn-press group relative inline-flex items-center gap-2 rounded-full px-7 py-3.5 text-base font-semibold text-white overflow-hidden transition-transform duration-150 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-400 focus-visible:ring-offset-2 focus-visible:ring-offset-[#FAFAF8]"
          >
            <div class="absolute inset-0 bg-gradient-to-r from-primary-600 to-primary-500"></div>
            <div class="absolute inset-0 bg-gradient-to-r from-primary-500 via-purple-500 to-primary-400 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
            <div class="absolute inset-0 rounded-full shadow-lg shadow-primary-600/25"></div>
            <span class="relative z-10">Choisir ma formation</span>
            <svg class="relative z-10 w-4 h-4 group-hover:translate-x-0.5 transition-transform duration-200" viewBox="0 0 16 16" fill="none">
              <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
          </a>

          <a href="/prendre-rdv" class="btn-press inline-flex items-center gap-2 rounded-full border border-gray-300 bg-white px-7 py-3.5 text-base font-semibold text-gray-700 hover:bg-gray-50 hover:border-gray-400 transition-colors duration-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-400/40 focus-visible:ring-offset-2 focus-visible:ring-offset-[#FAFAF8]">
            Reserver mon appel gratuit
          </a>
        </div>

        <!-- Micro-copy reassurance -->
        <p class="reveal reveal-delay-3 mt-3 text-xs text-gray-400">Sans engagement &middot; Conseiller dedie &middot; Reponse en 24h</p>

        <!-- Google Reviews Badge -->
        <div class="reveal reveal-delay-4 mt-6">
          <a href="/#avis" class="inline-flex items-center gap-3 rounded-2xl bg-white shadow-sm border border-gray-200/60 px-5 py-3 hover:shadow-md transition-shadow duration-200 group focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-400/60">
            <div class="flex items-center gap-0.5">
              {[1, 2, 3, 4, 5].map(() => (
                <svg class="w-4 h-4 text-yellow-400" viewBox="0 0 20 20" fill="currentColor">
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                </svg>
              ))}
            </div>
            <span class="text-sm font-bold text-gray-900">5/5</span>
            <div class="w-px h-4 bg-gray-200"></div>
            <span class="text-sm text-gray-500">172+ avis</span>
            <svg class="w-4 h-4 ml-1" viewBox="0 0 24 24">
              <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92a5.06 5.06 0 01-2.2 3.32v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.1z" fill="#4285F4" />
              <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853" />
              <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z" fill="#FBBC05" />
              <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335" />
            </svg>
          </a>
        </div>
      </div>

      <!-- Right: Slideshow with photos -->
      <div class="flex-1 max-w-[560px] w-full reveal reveal-delay-2">
        <div class="relative">
          <!-- Glow behind cards -->
          <div class="absolute inset-0 bg-gradient-to-br from-primary-300/20 to-purple-300/15 blur-3xl rounded-full scale-110"></div>

          <div class="relative overflow-hidden rounded-3xl shadow-2xl shadow-gray-900/10" id="hero-slideshow">
            <div class="flex transition-transform duration-700 ease-[cubic-bezier(0.16,1,0.3,1)]" id="hero-slides">
              {slides.map((slide) => (
                <div class="min-w-full">
                  <div class="relative aspect-[4/3] rounded-3xl overflow-hidden">
                    <img
                      src={slide.img}
                      alt={`Portrait de ${slide.name}`}
                      class="absolute inset-0 w-full h-full object-cover"
                      width="600"
                      height="450"
                      loading="eager"
                    />
                    <!-- Overlay gradient for text readability -->
                    <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/10 to-transparent"></div>
                    <div class="absolute bottom-0 left-0 right-0 p-8">
                      <p class="text-xl font-heading font-bold text-white mb-1">{slide.name}</p>
                      <p class="text-sm text-white/80 max-w-xs leading-relaxed">{slide.caption}</p>
                    </div>
                  </div>
                </div>
              ))}
            </div>
            <!-- Dots -->
            <div class="absolute bottom-5 right-8 flex gap-2">
              {slides.map((_, i) => (
                <button
                  class={`h-1.5 rounded-full transition-all duration-300 ${i === 0 ? "bg-white w-6" : "bg-white/40 w-1.5 hover:bg-white/70"}`}
                  data-slide={i}
                  aria-label={`Slide ${i + 1}`}
                />
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const slides = document.getElementById("hero-slides");
  const dots = document.querySelectorAll("[data-slide]");
  let current = 0;
  const total = dots.length;

  function goTo(i: number) {
    current = i;
    if (slides) slides.style.transform = `translateX(-${i * 100}%)`;
    dots.forEach((d, idx) => {
      if (idx === i) {
        d.className = "h-1.5 rounded-full transition-all duration-300 bg-white w-6";
      } else {
        d.className = "h-1.5 rounded-full transition-all duration-300 bg-white/40 w-1.5 hover:bg-white/70";
      }
    });
  }

  dots.forEach((d) => d.addEventListener("click", () => goTo(Number(d.getAttribute("data-slide")))));
  setInterval(() => goTo((current + 1) % total), 6000);
</script>
